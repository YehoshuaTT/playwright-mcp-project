{
  "mcpServers": {
    "playwright": {
      "command": "node",
      "args": ["-e", "const WebSocket = require('ws'); const ws = new WebSocket('ws://localhost:3001'); ws.on('open', () => { console.log('Connected to Playwright MCP'); process.stdin.pipe(ws); ws.pipe(process.stdout); });"],
      "env": {
        "NODE_PATH": "C:\\Users\\%USERNAME%\\AppData\\Roaming\\npm\\node_modules"
      }
    }
  }
}